
⚠️ Lệnh này xóa toàn bộ folder và tất cả dữ liệu bên trong:

rm -r ten_folder

Ví dụ:

rm -r myapp
3. Xóa bắt buộc, không hỏi
rm -rf ten_folder

Ví dụ:

sudo rm -rf /opt/myapp

-r = xóa cả thư mục con và file bên trong
-f = ép xóa, không hỏi xác nhận
sudo = dùng quyền quản trị
